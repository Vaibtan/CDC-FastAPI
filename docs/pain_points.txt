The plan now explicitly calls out the async/sync mismatch as CRITICAL and provides a correct aiokafka-based async Kafka replay implementation (including async iteration and timestamp seeking). It also adds a walstream-common library to share dedup/checkpoint models across services, which addresses the cross-service import/coupling concern.​​
Issues still not resolved (in the implementation sections)

Even though the “Critical Implementation Notes” section shows the corrected aiokafka approach, the Phase 3 worker and replay-router snippets still instantiate KafkaConsumer from kafka-python and show the blocking for message in self.consumer iteration pattern inside async-shaped code paths. Concretely, initdependencies() in the JobWorker still uses from kafka import KafkaConsumer, and the Phase 3 KafkaReplaySource section still contains synchronous-consumer logic (timestamp checks and iteration) that would block an asyncio worker.​

Dedup location is also still mixed: the architecture decision table says “Dedup location: Replayer centralized,” yet the worker’s replayevent() checks self.dedupstore.exists() before calling the Replayer and then calls self.dedupstore.markprocessed() after a successful gRPC replay. Meanwhile, the Replayer section still describes doing the dedup check and “mark processed” inside the Replayer itself.​
Choices that are not optimal (or need tightening)

The ingestor Kafka producer configuration uses enableidempotenceTrue (no underscore), while the kafka-python configuration option is documented as enable_idempotence (with an underscore). Even if idempotent producing is desired, it’s safer to standardize on one Kafka client library across the system (e.g., aiokafka end-to-end, or confluent-kafka for producer/consumer) to avoid subtle behavioral differences and config drift.​​

Also, dbinit.sql includes CREATE PUBLICATION ..., but the ingestion design is explicitly based on logical decoding with the wal2json output plugin (replication slot + decoding), where publications are a pgoutput/logical replication concept rather than a wal2json requirement.​​
Concrete edits to make it “done”

    Make the Phase 3 code match the “Critical Notes”: replace kafka.KafkaConsumer usage in the worker/router with an aiokafka.AIOKafkaConsumer implementation (or isolate kafka-python behind run_in_executor, but the doc itself recommends native async).​

    Choose one dedup enforcement point and delete the other:

        If “Replayer centralized” is the goal, remove self.dedupstore.exists() / markprocessed() from the worker and rely only on the Replayer’s dedup repo + wasduplicate response.​

        If worker-side dedup is desired for load shedding, then update the architecture decision table and ensure the Replayer does not also mark processed (or make Replayer idempotent but treat worker-side as a cache).​

    Fix Kafka producer config naming (enable_idempotence) and confirm the chosen library supports it as intended.​​

    Clarify the Postgres setup: either keep publications only if switching to pgoutput, or remove publication steps if staying on wal2json-based logical decoding.​​

If a single choice must be made for “optimal”: keep dedup strictly in the Replayer (single writer to dedup store), and make Kafka consumption strictly aiokafka in all async services so the operational model stays simple and reliable.